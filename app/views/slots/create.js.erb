$(".slot_form").hide();
$('#calendar').fullCalendar( 'refetchEvents');
$(".modal-events").modal('hide');

var truc = '<%= @errors %>';
if (truc) {
  alert ('Cannot create slot because ' + truc);
}




