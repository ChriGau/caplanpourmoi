var errors = '<%= @errors %>';
if (errors) {
  $('.modal-body').prepend("<b style='color: red;'>Can't save slot because: " + errors+ "</b>");
} else {
  $(".slot_form").hide();
  $('#calendar').fullCalendar( 'refetchEvents');
  $(".modal-events").modal('hide');
}





