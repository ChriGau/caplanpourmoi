<h4>Problème! pas de disponibilités de l'équipe</h4>
<h4>Autres personnes disponibles :</h4>
<%= simple_form_for [@planning, @slot], remote: true do |f| %>
<%= f.error_notification %>
<%= f.input :user_id, placeholder: "User", label: false, collection: fetch_user_solution, prompt: "Choisir le backup", label_method: lambda { |guy| "#{guy.first_name.capitalize}"}  %>
<%= f.submit "Affecter", class: "btn btn-success" %>
<%= link_to "Annuler", :back, {:class=>"btn btn-default"} %>
<% end %>
