  <%= render 'shared/planningsteps', locals: {planning: @planning} %>

  <%= simple_form_for (@planning) do |f| %>
  <%= f.association :users, collection: @users, label_method: :first_name, as: :check_boxes %>
  <%= f.button :submit %>
  <% end %>
